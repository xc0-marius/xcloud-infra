# TODO: Replace with your real NetBird reverse-proxy environment.
# Expected DNS: *.netbird.xcloud.gg -> 46.225.19.105
#
# On the server, prepare.sh creates:
#   netbird/proxy.env
#
# That file is gitignored because it can contain private configuration.
